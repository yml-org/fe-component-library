import { html } from 'lit';
import { Story, Meta, Canvas } from '@storybook/addon-docs';
import '../../../header/src/header/header.component.ts';
import './button.component.ts';

<Meta title="Button" />

# Button Component

Basic Button component

export const defaultTemplate = () =>
  html`<button-component ></button-component>`;

<Canvas>
  <Story
    name="default"
  >
    {defaultTemplate.bind({})}
  </Story>
</Canvas>

Button component With `text` prop

export const withTextTemplate = ({text}) =>
  html`<button-component .text=${text}></button-component>`;

<Canvas>
  <Story
    name="with text"
    args={{
        text:"YML"
    }}
  >
    {withTextTemplate.bind({})}
  </Story>
</Canvas>

Button component With `link` Variant

export const withVariantTemplate = ({ variant}) =>
  html`<button-component  .variant=${variant}></button-component>`;

<Canvas>
  <Story
    name="with link variant"
    args={{
        variant:"link"
    }}
  >
    {withVariantTemplate.bind({})}
  </Story>
</Canvas>

Button component With `Contained` Variant

<Canvas>
  <Story
    name="with contained variant"
    args={{
        variant:"contained"
    }}
  >
    {withVariantTemplate.bind({})}
  </Story>
</Canvas>

Button component With `outlined` Variant

<Canvas>
  <Story
    name="with outlined variant"
    args={{
        variant:"outlined"
    }}
  >
    {withVariantTemplate.bind({})}
  </Story>
</Canvas>

Button component With `Primary` color

export const withColorTemplate = ({color}) =>
  html`<button-component .color=${color}></button-component>`;

<Canvas>
  <Story
    name="with primary color"
    args={{
        color:"primary"
    }}
  >
    {withColorTemplate.bind({})}
  </Story>
</Canvas>

Button component With `Secondary` color

<Canvas>
  <Story
    name="with secondary color"
    args={{
        color:"secondary"
    }}
  >
    {withColorTemplate.bind({})}
  </Story>
</Canvas>


Button component With `success` color

<Canvas>
  <Story
    name="with success color"
    args={{
        color:"success"
    }}
  >
    {withColorTemplate.bind({})}
  </Story>
</Canvas>

Button component With `danger` color

<Canvas>
  <Story
    name="with danger color"
    args={{
        color:"danger"
    }}
  >
    {withColorTemplate.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="with warning color"
    args={{
        color:"warning"
    }}
  >
    {withColorTemplate.bind({})}
  </Story>
</Canvas>


Button component With `light` color

<Canvas>
  <Story
    name="with light color"
    args={{
        color:"light"
    }}
  >
    {withColorTemplate.bind({})}
  </Story>
</Canvas>


Button component With `dark` color

<Canvas>
  <Story
    name="with dark color"
    args={{
        color:"dark"
    }}
  >
    {withColorTemplate.bind({})}
  </Story>
</Canvas>


Button component With `round` border

export const withRoundedTemplate = ({isRounded}) =>
  html`<button-component .isRounded=${isRounded}></button-component>`;

<Canvas>
  <Story
    name="with rounded prop"
    args={{
        isRounded:true
    }}
  >
    {withRoundedTemplate.bind({})}
  </Story>
</Canvas>


Button component With `disabled` property

export const withDisableTemplate = ({isDisabled}) =>
  html`<button-component .isDisabled=${isDisabled}></button-component>`;

<Canvas>
  <Story
    name="with disabled prop"
    args={{
        isDisabled: true
    }}
  >
    {withDisableTemplate.bind({})}
  </Story>
</Canvas>


Button component With `full-width`

export const withFullWidthTemplate = ({fullWidth}) =>
  html`<button-component .fullWidth=${fullWidth}></button-component>`;

<Canvas>
  <Story
    name="with fullWidth"
    args={{
        fullWidth: true
    }}
  >
    {withFullWidthTemplate.bind({})}
  </Story>
</Canvas>


Button component With `Slot`  

export const withSlotTemplate = ({text}) =>
  html`<button-component .text=${text}>
  <a href="#" slotName="text">A Link</a></button-component>`;

<Canvas>
  <Story
    name="with slot"
    args={{
        slotName:"text",
        text:""
    }}
  >
    {withSlotTemplate.bind({})}
  </Story>
</Canvas>