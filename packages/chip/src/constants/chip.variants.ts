import { ChipVariant } from '../types/chip';

export const withSlot: ChipVariant = {
  rightSlotName: 'closeIcon',
  leftSlotName: 'menuIcon',
  chipPartAttribute : 'customizeChip'
};

export const withTextProp : ChipVariant = {
  text : 'Chip Component',
}
