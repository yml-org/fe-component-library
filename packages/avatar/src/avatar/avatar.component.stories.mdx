import { html } from 'lit';
import { Story, Meta, Canvas } from '@storybook/addon-docs';
import './avatar.component.ts';
import {
  roundedVariant,
  withDefaultIcon,
  withCustomHeightWidth,
  withRoundedBorderAvatar,
  withShadow,
  withSlot,
} from '../constants/avatar.variants.ts';
import './avatar.component.scss';

<Meta title="Components/Avatar" />

# Avatar Component

The Avatar component is a Lit component that renders an avatar component that is framework independent and utilizes Tailwind CSS internally.

# Props

| Prop Name       | Type     | Description                                                              |
| --------------- | -------- | ------------------------------------------------------------------------ |
| isRounded       | boolean  | Flag to determine whether or not to have rounded borders                 |
| width           | number   | The width of the avatar component                                        |
| height          | number   | The height of the avatar component                                       |
| usePercentage   | boolean  | Flag to determine whether or not the dimensions should be in percentages |
| hasBorder       | boolean  | Flag to determine whether the border is shown or not                     |
| borderColor     | string   | The border color                                                         |
| slotName        | string   | The name of the child slot                                               |
| onAvatarClick   | Function | The click function that gets triggered on avatar component click         |
| hasShadow       | boolean  | Flag to determine whether the avatar component has box shadow or not     |
| showDefaultIcon | boolean  | Flag to determine whether tho show default avatar icon or not            |
| defaultImage    | string   | The default image to be displayed                                        |
| defaultBgColor  | string   | The default background color provided by the user                        |

# Examples

Avatar component with rounded border with default background

export const roundedBorderTemplate = ({ isRounded, defaultBgColor }) =>
  html`<avatar-component
    .isRounded=${isRounded}
    .defaultBgColor=${defaultBgColor}
  ></avatar-component>`;

<Canvas>
  <Story name="rounded border" args={roundedVariant}>
    {roundedBorderTemplate.bind({})}
  </Story>
</Canvas>

Avatar component with default icon

export const defaultIconTemplate = ({ showDefaultIcon, defaultImage }) =>
  html`<avatar-component
    .showDefaultIcon=${showDefaultIcon}
    .defaultImage=${defaultImage}
  ></avatar-component>`;

<Canvas>
  <Story name="default icon" args={withDefaultIcon}>
    {defaultIconTemplate.bind({})}
  </Story>
</Canvas>

Avatar component with custom width and height

export const withCustomWidthHeightTemplate = ({
  showDefaultIcon,
  defaultImage,
  width,
  height,
}) =>
  html`<avatar-component
    .showDefaultIcon=${showDefaultIcon}
    .defaultImage=${defaultImage}
    .width=${width}
    .height=${height}
  ></avatar-component>`;

<Canvas>
  <Story name="custom height and width" args={withCustomHeightWidth}>
    {withCustomWidthHeightTemplate.bind({})}
  </Story>
</Canvas>

Avatar component with border

export const withRoundedBorderAvatarTemplate = ({
  showDefaultIcon,
  defaultImage,
  isRounded,
  hasBorder,
}) =>
  html`<avatar-component
    .showDefaultIcon=${showDefaultIcon}
    .defaultImage=${defaultImage}
    .isRounded=${isRounded}
    .hasBorder=${hasBorder}
  ></avatar-component>`;

<Canvas>
  <Story
    name="with rounded border avatar variant"
    args={withRoundedBorderAvatar}
  >
    {withRoundedBorderAvatarTemplate.bind({})}
  </Story>
</Canvas>

Avatar component with shadow and border

export const withShadowTemplate = ({
  showDefaultIcon,
  defaultImage,
  isRounded,
  hasBorder,
  hasShadow,
}) =>
  html`<avatar-component
    .showDefaultIcon=${showDefaultIcon}
    .defaultImage=${defaultImage}
    .isRounded=${isRounded}
    .hasBorder=${hasBorder}
    .hasShadow=${hasShadow}
  ></avatar-component>`;

<Canvas>
  <Story name=" avatar with shadow" args={withShadow}>
    {withShadowTemplate.bind({})}
  </Story>
</Canvas>

Avatar component with slot and custom children

export const withSlotAndChildrenTemplate = ({
  isRounded,
  hasBorder,
  hasShadow,
  slotName,
}) =>
  html`<avatar-component
    .slotName=${slotName}
    .isRounded=${isRounded}
    .hasBorder=${hasBorder}
    .hasShadow=${hasShadow}
  >
    <div slot=${slotName}>${'YML'.charAt(0)}</div>
  </avatar-component>`;

<Canvas>
  <Story name="with slot and children" args={withSlot}>
    {withSlotAndChildrenTemplate.bind({})}
  </Story>
</Canvas>

Avatar component with custom styling

export const withCustomStylingTemplate = ({
  isRounded,
  hasBorder,
  hasShadow,
  slotName,
}) =>
  html` <div class="parent-container">
    <avatar-component
      .slotName=${slotName}
      .isRounded=${isRounded}
      .hasBorder=${hasBorder}
      .hasShadow=${hasShadow}
    >
      <div slot=${slotName}>
        <span> ${'YML'.charAt(0)}</span>
      </div>
    </avatar-component>
  </div>`;

<Canvas>
  <Story name="with custom styling" args={withSlot}>
    {withCustomStylingTemplate.bind({})}
  </Story>
</Canvas>
